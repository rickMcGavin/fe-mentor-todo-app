import React from "react";
import * as styles from "./styles";

const ThemeToggle = () => {
  return <styles.ThemeToggle onClick={() => console.log("mode toggle")} />;
};

export default ThemeToggle;
